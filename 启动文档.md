# AlphaTrace Agent 本地启动文档

这份文档用于本地调试前后端 MVP demo。默认使用 mock 模式，不需要真实 Arc Testnet 合约也能走通完整流程。

## 1. 环境要求

- Python: conda 环境 `py3_11_arc`
- Node.js: 当前已验证 `v24.15.0` 可 build
- 后端端口: `8000`
- 前端端口: 默认 `3000`，如果被占用 Next.js 会自动切到 `3001`

## 2. 后端启动

进入项目根目录：

```bash
cd /home/minner/Codes/circle-arc/AlphaTrace-Agent
```

激活 Python 环境：

```bash
conda activate py3_11_arc
```

首次运行时安装依赖：

```bash
cd backend
python -m pip install -r requirements.txt
```

创建本地环境变量文件：

```bash
cp .env.example .env
```

如果使用本地 PostgreSQL，配置：

```env
DATABASE_URL=
PSQL_DB_HOST=localhost
PSQL_DB_PORT=5432
PSQL_DB_USER=alpha_user
PSQL_DB_PASSWORD=12345678
PSQL_DB_DATABASE=alpha_trace
```

后端会优先使用完整的 `PSQL_DB_*` 配置；如果没有配置 PostgreSQL，才会使用 `DATABASE_URL` 或本地 SQLite。

如果启动时报 `permission denied for schema public`，用 PostgreSQL 管理员账号执行：

```sql
GRANT CONNECT ON DATABASE alpha_trace TO alpha_user;
GRANT USAGE, CREATE ON SCHEMA public TO alpha_user;
ALTER DEFAULT PRIVILEGES IN SCHEMA public
GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO alpha_user;
ALTER DEFAULT PRIVILEGES IN SCHEMA public
GRANT USAGE, SELECT, UPDATE ON SEQUENCES TO alpha_user;
```

如果前端跑在 `3001`，把 `backend/.env` 里的 `CORS_ORIGINS` 改成：

```env
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001
```

启动后端：

```bash
python -m uvicorn app.main:app --reload --port 8000
```

健康检查：

```bash
curl http://127.0.0.1:8000/health
```

正常返回：

```json
{"status":"ok","service":"alphatrace-agent"}
```

## 3. 前端启动

新开一个终端，进入前端目录：

```bash
cd /home/minner/Codes/circle-arc/AlphaTrace-Agent/frontend
```

首次运行时安装依赖：

```bash
npm install
```

创建本地环境变量文件：

```bash
cp .env.local.example .env.local
```

确认 `frontend/.env.local` 里后端地址是：

```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```

启动前端：

```bash
npm run dev
```

打开终端输出里的地址，通常是：

```text
http://localhost:3000
```

如果 `3000` 被占用，Next.js 会提示并切到：

```text
http://localhost:3001
```

## 4. Demo 调试流程

1. 打开前端首页。
2. 进入 `Create` 页面。
3. 保持 `Demo sync` 模式。
4. 点击 `Create Job`。
5. 进入 Job Detail 页面。
6. 点击 `Fund Job`，Job 状态变为 `funded`。
7. 点击 `Run Agent`，后端生成结构化报告和 report hash，Job 状态变为 `submitted`。
8. 查看报告、评分、证据和 hash verified 状态。
9. 点击 `Complete Job`，Job 状态变为 `completed`。
10. 提交 Feedback。

## 5. 常用检查命令

后端语法检查：

```bash
cd /home/minner/Codes/circle-arc/AlphaTrace-Agent
python -m compileall backend/app
```

前端生产构建：

```bash
cd /home/minner/Codes/circle-arc/AlphaTrace-Agent/frontend
npm run build
```

查看当前 git 改动：

```bash
git status --short
```

## 6. 常见问题

### 前端请求后端时报 CORS 或 OPTIONS 400

通常是前端跑到了 `3001`，但后端只允许 `3000`。

修改 `backend/.env`：

```env
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001
```

然后重启后端。

### 前端提示 3000 被占用

这是正常的。Next.js 会自动尝试 `3001`。

如果你想固定端口：

```bash
npm run dev -- -p 3001
```

### 后端提示 `No module named uvicorn`

说明当前 conda 环境还没安装后端依赖，执行：

```bash
cd backend
python -m pip install -r requirements.txt
```

### 前端 dev 日志出现 RainbowKit / WalletConnect 相关可选依赖 warning

如果页面能正常打开，可以先忽略。若 dev server 因缺包失败，可安装：

```bash
npm install @react-native-async-storage/async-storage pino-pretty
```

### 真实链上模式

默认本地 demo 使用 mock 模式：

```env
SUBMIT_TO_CHAIN=false
```

等 Arc Testnet 合约、Agent 钱包和 USDC 配置好后，再填写 `backend/.env` 和 `frontend/.env.local` 里的合约地址、RPC、chain id、agent 地址等字段，并切换前端到 `Create on Arc`。

## 7. 注册真实 ERC-8004 Agent

项目提供了一个脚本用于在 Arc Testnet 的 ERC-8004 `IdentityRegistry` 上注册 AlphaTrace Agent。

先确认 `backend/.env` 至少填好：

```env
ARC_RPC_URL=你的_Arc_Testnet_RPC
ARC_CHAIN_ID=5042002
PRIVATE_KEY_AGENT=你的_agent_owner_wallet_private_key
IDENTITY_REGISTRY_ADDRESS=0x8004A818BFB912233c491871b3d84c89A494BD9e
ALPHATRACE_AGENT_METADATA_URI=ipfs://alphatrace-demo-agent
```

注意：`PRIVATE_KEY_AGENT` 对应的钱包需要有 Arc Testnet gas token。

先 dry run 检查配置：

```bash
cd /home/minner/Codes/circle-arc/AlphaTrace-Agent
conda activate py3_11_arc
python backend/scripts/register_agent.py --dry-run
```

真正提交注册交易，并自动回写 `backend/.env` 和 `frontend/.env.local`：

```bash
python backend/scripts/register_agent.py --write-env
```

如果你已经在 `backend/.env` 配置了 Pinata：

```env
PINATA_JWT=你的_Pinata_JWT
```

也可以让脚本先生成 Agent metadata JSON，上传到 Pinata，再用返回的 `ipfs://CID` 注册：

```bash
python backend/scripts/register_agent.py --pinata-upload --write-env
```

如果你想上传自己写好的 metadata JSON：

```bash
python backend/scripts/register_agent.py --pinata-upload --metadata-file backend/agent-metadata.json --write-env
```

脚本成功后会输出：

```text
ALPHATRACE_AGENT_ADDRESS=0x...
ALPHATRACE_AGENT_ID=...
```

其中 `ALPHATRACE_AGENT_ADDRESS` 就是前端需要的：

```env
NEXT_PUBLIC_ALPHATRACE_AGENT_ADDRESS=0x...
```

注册完成后重启前后端，让新的环境变量生效。
